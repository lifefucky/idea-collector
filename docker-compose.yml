services:
  idea-collector:
    build: .
    image: idea-collector
    env_file: .env
    environment:
      SQLITE_PATH: /app/data/ideas.db
      PORT: "8080"
    volumes:
      - data:/app/data
    ports:
      - "8080:8080"

volumes:
  data:
    name: idea-collector-data
